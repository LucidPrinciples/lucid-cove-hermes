# Generate:
#   openssl rand -hex 32
BETTER_AUTH_SECRET=

PAPERCLIP_DEPLOYMENT_MODE=local_trusted
PAPERCLIP_PUBLIC_URL=http://127.0.0.1:3100
PAPERCLIP_TELEMETRY_DISABLED=1

# Optional cloud keys for Paperclip-native features (not required for hermes_gateway + local Ollama)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
