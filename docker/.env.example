# Copy to .env on the host if you need compose-time overrides.
# Secrets for Hermes itself live in ../data/.env (Hermes home), not here.

HERMES_UID=1000
HERMES_GID=1000

# Optional later (do not enable on day one):
# API_SERVER_KEY=
# API_SERVER_HOST=127.0.0.1
