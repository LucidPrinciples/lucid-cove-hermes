# Pack

SOULs, skills, and Lucid Principles spine for a Lucid Cove on Hermes house.

Apply with `scripts/sync_pack_to_hermes.sh` from the repo root. Set `LCH_ROOT` if the overlay is not the script's parent directory.

Do not put hostnames, tokens, or a live vault in this tree.
